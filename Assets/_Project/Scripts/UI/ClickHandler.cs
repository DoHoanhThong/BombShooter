using UnityEngine;

public class ClickHandler : MonoBehaviour
{
    private void Update()
    {
        //if (Input.GetMouseButtonDown(0) && GameController.Instance.isHomeScene)
        //{
        //    Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
        //    RaycastHit2D hit = Physics2D.Raycast(ray.origin, ray.direction);

        //    if (hit.collider != null)
        //    {
        //        var hitObj = hit.collider.GetComponent<RequestBubble>();
        //        if (hitObj != null)
        //        {
        //            hit.collider.GetComponent<RequestBubble>().OnMouseDown();
        //        }
        //    }
        //}
    }
}
